# DAG Editor

一个纯前端的有向图编辑应用，支持：

- 新建多个画布；
- 在画布中新增节点、拖动节点；
- 从节点右侧句柄拖拽到目标节点创建有向边；
- 点击节点编辑名称、颜色与自定义 JSON 属性；
- 将当前图导出为 JSON 文件。

## 运行

直接在浏览器打开 `index.html`，或使用本地静态服务器：

```bash
python3 -m http.server 4173
```

访问 <http://localhost:4173>。
